#include "main.h"

/**
 * print_alphabet - print the alpha in lowercase
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	char la;

	for (la = 'a'; la <= '2'; la++)
		_putchar(la);

	_putchar('\n');
